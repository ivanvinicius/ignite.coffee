import { styled } from '../../../styles/theme'

export const FlatContainer = styled('div', {
  padding: '$32 0',
  width: '100%',
  height: '100%',
  display: 'flex',
  alignItems: 'center',
  gap: '$32',
  overflowX: 'auto',
  scrollSnapType: 'x mandatory'
})
